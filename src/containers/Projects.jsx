import React from 'react'

export const Projects = ({children}) => {
  return (
    <section id='projects' className='w-full h-auto bg-[#160d45] text-white py-8 lg:px-12'>
      {children}
    </section>
  )
}
